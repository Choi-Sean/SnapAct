// Catalog/ — the action catalog (step 3).
//
// Parses Resources/actions.json, which tools/build_catalog.py generates from
// docs/SnapAct_클래스별액션_검토.xlsx. No class list or action list is ever
// hardcoded here: editing the spreadsheet and re-running the script is the
// only way the catalog changes.
