# SnapActKit 카탈로그 생성 도구 전용.
#
# 일부러 가볍게 유지합니다 — build_catalog.py 는 xlsx 를 읽어 JSON 을 쓰는 것이
# 전부라서 torch 도 coremltools 도 필요 없습니다. 카탈로그만 다시 만들려는
# 사람이 1GB 짜리 학습 스택을 받지 않아도 되게 이 파일을 따로 둡니다.
openpyxl>=3.1
